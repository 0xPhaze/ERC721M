{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "test",
            "type": "shell",
            // "command": "forge test -vvv --no-match-contract StakingGasTest",
            "command": "forge snapshot --match-contract StakingGasTest",
            "group": {
				"kind": "test",
				"isDefault": true
			},
        }
    ]
}